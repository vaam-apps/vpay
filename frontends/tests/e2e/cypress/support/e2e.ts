// Shared e2e setup. Intentionally empty: no request stubbing lives here.
// The rails are stubbed by WireMock at the infrastructure layer (compose.yml),
// not by intercepting inside the browser — see docs/adr/0006.
export {};
