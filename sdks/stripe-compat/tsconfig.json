{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    // Node-only, like @vpay/sdk itself: no DOM lib, `fetch` comes from
    // @types/node.
    "lib": ["ES2023"],
    "noEmit": true,
    "types": ["node"]
  },
  "include": ["src", "vitest.config.ts"]
}
