<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      `android:usesCleartextTraffic="true"` — this example app is pointed at
      `compose.demo.yml` (this package's own doc comment, and
      `example/lib/main.dart`'s), which is plain HTTP by design. Android
      blocks cleartext by default for any `targetSdk` >= 28
      (`NetworkSecurityConfig`'s own default), which would otherwise make
      `VpayCheckoutActivity`'s `WebView` fail every `loadUrl` here silently
      — not a certificate error, just no navigation at all, which is
      indistinguishable from the page never having loaded. Mirrors the
      Dart-side `allowInsecureBaseUrl`/`allowInsecureUrl` opt-in this
      package already requires for the same demo stack; it belongs at the
      APP level (never in `android/src/main/AndroidManifest.xml`, which
      ships to every consumer of this plugin, not only this example).
    -->
    <application
        android:label="vpay_checkout_flutter_example"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
