# vpay Helm chart.
#
# Renders the two shipping workloads — the API server and the background
# worker, **one image between them** since issue #77 (2026-09-07): the worker
# Deployment runs the `vpay-server` image with `args: ["worker"]` — and
# the objects around them. It deliberately templates **no database**: ADR-0013
# / step-6 decision (9) puts Postgres outside this chart, reached through an
# existing Secret. See README.md.
#
# NOTHING IN THIS CHART HAS EVER BEEN APPLIED TO A CLUSTER. `helm lint`,
# `helm template` and `kubeconform` are the whole of its evidence. Read the
# Status section of README.md before you deploy it.
apiVersion: v2
name: vpay
description: vpay payment gateway — API server and background worker
type: application

# Chart version. Bumped by hand; there is no release automation for the chart
# itself yet (the container images are block A's `release.yml`, this is not).
#
# 0.1.0 -> 0.2.0 on 2026-09-07 (issue #77). A minor bump rather than a patch
# because the rendered output changes and a values file can break: the worker
# Deployment's `image:` now resolves from `images.server` and it gained
# `args: ["worker"]`, and `images.worker` was REMOVED from `values.yaml` and
# from `values.schema.json` — so a values file that pinned
# `images.worker.digest` fails `helm lint` (the schema is
# `additionalProperties: false`) instead of pinning a digest nothing pulls.
#
# 0.2.0 -> 0.2.1 on 2026-09-18. A patch: nothing rendered changes shape, but
# what a default install PULLS does. `appVersion` moved 0.1.0 -> 0.1.1 with
# the `v0.1.1` release, and the block below is the rule that makes that one
# edit rather than two — `values.yaml`'s `images.*.tag` defaults to
# `appVersion`, so a chart left at 0.2.0 now resolves to a different image
# than the 0.2.0 anyone already has. #204 restored this file and moved
# `appVersion` without moving this line; that half is finished here.
#
# THIS LINE READ `0.1.1` ON `master` FROM 2026-09-17 UNTIL #204 — a downgrade
# nobody typed, and the chart behind its own app. `#203`, the first
# release-please release, did it, and the mechanism is in
# `release_please_extra_files`' doc comment and AGENTS.md § Releasing: a
# bare-string `extra-files` entry gets an updater chosen from the file
# extension, and `.yaml` gets one that reparses and re-serialises the whole
# document. Every entry is `{"type": "generic"}` now and `cargo xtask
# verify-versions` refuses a bare string outright.
version: 0.2.1

# The application version this chart was written against. It is NOT a pin:
# `values.yaml`'s `images.*.tag` defaults to this string, so a chart bump and
# an image bump are the same edit unless a tag or digest is set explicitly.
appVersion: "0.2.2" # x-release-please-version

# `seccompProfile` on a pod spec (used by both Deployments) is GA from 1.19,
# `PodDisruptionBudget` in `policy/v1` from 1.21, and the
# `matchLabelKeys`-free NetworkPolicy shapes used here are far older. 1.27 is
# the floor chosen because it is the oldest release still receiving patches
# when this chart was written (2026-09-03) — not because anything here was
# tested against it. Nothing here has been tested against any version.
kubeVersion: ">=1.27.0-0"

home: https://github.com/vaam-apps/vpay
sources:
  - https://github.com/vaam-apps/vpay
keywords:
  - payments
  - mobile-money
