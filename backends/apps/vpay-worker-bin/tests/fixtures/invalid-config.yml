# Fixture for `a_bad_config_causes_a_non_zero_exit_naming_the_problem`
# (`backends/apps/vpay-worker-bin/tests/cli.rs`). Mirrors
# `backends/apps/vpay-server/tests/fixtures/invalid-config.yml` — an
# `http://` host under `livemode: true` must be rejected by
# `vpay_config::validate_host` (`ConfigError::InsecureHost`) before this
# process ever touches the database.

deployment:
  name: vpay-worker-cli-test-invalid
  livemode: true
  public_base_url: https://api.vpay.example

providers:
  - code: mtn_momo
    host:
      url: http://proxy.momoapi.mtn.com
      label: mtn-cm-prod
