# Fixture for `a_livemode_config_with_a_literal_secret_is_rejected`
# (backends/crates/vpay-config/src/config.rs). The host is https and not
# stub-labelled, so validate_host passes; the point of this fixture is the
# literal (non-`${VAR}`) credential value below.

deployment:
  name: prod
  livemode: true
  public_base_url: https://api.vpay.example

providers:
  - code: mtn_momo
    host:
      url: https://proxy.momoapi.mtn.com
      label: mtn-cm-prod
    credentials:
      api_key: hunter2
