# 2026-09-16 — ADR-0022 (surface isolation and independent scaling)

## What this covers

Commits 1–3 of the ADR-0022 implementation branch
(`claude/api-architecture-refactor-a12be3`): the `deployment.surfaces`
config selector and conditional `/v1`/`/dash/v1` mounting (Rust), the
dashboard runner stage hardening and its read-only observation (Docker), and
the Helm chart's `-management`/`-dashboard` workloads, HPA and
`connection-budget` guard. Not commit 4 — this page is that commit's own
evidence.

## Commit 1 — Rust

```
cargo test -p vpay-config --lib
```

120 passed; 0 failed; 0 ignored (includes 6 new tests:
`absent_surfaces_means_every_surface`, `one_named_surface_enables_only_that_one`,
`both_named_explicitly_is_the_same_as_absent`,
`deployment_surfaces_empty_is_a_boot_error`,
`deployment_surfaces_unknown_value_is_a_boot_error`, plus the `pdb`-adjacent
ones already there).

```
cargo test -p vpay-api --lib tests::surfaces
```

4 passed; 0 failed (the four scenarios the brief named: `surfaces:
[management]` unmounts `/v1` and keeps `/dash/v1`; `surfaces: [business]`
unmounts `/dash/v1` and keeps `/v1`; absent mounts both, unchanged from
before this ADR — the backward-compatibility case; and the empty-list
refusal, asserted at the `vpay_config` boundary with an `exit_code() == 78`
check).

Whole-workspace run, later the same session:

```
just ci
```

- `fmt-check`, `clippy`, `verify` (12 gates): all passed.
- `test-rust` (`cargo nextest`): **1838/1838 passed, 0 skipped.**
- `test-doc` (`cargo test --doc --workspace`, a separate runner from
  `nextest`): **114 passed, 1 ignored, 0 failed**, across
  `vpay_adapter_mtn_momo` (1), `vpay_adapter_orange_money` (1), `vpay_api`
  (18), `vpay_config` (5), `vpay_core` (56), `vpay_db` (8), `vpay_ledger`
  (2), `vpay_provider` (10), `vpay_sdk` (8, 1 ignored), `vpay_server` (0),
  `vpay_testkit`/`vpay_tests_conformance`/`vpay_tests_integration` (0 each —
  no doctests in those crates), `vpay_worker` (5).
- `verify-ignored`: 0 unexpectedly-ignored, 48 test binaries, 1838 total.
- `lint-web`, `test-web`, `audit-web`: all passed (this run's tree already
  included `frontends/apps/dashboard/app/healthz/route.ts`, so its
  `eslint`/`tsc`/`prettier` results are covered here too).
- `deny`: **FAILED**, but not on anything this branch touched — `advisories
FAILED` on `RUSTSEC-2026-0285` (a TLS 1.3 handshake-message vulnerability
  in `rustls 0.23.43`), reached only through `bollard` → `testcontainers` →
  `vpay-testkit`, a **dev-only** dependency chain. `git diff` against this
  branch's base carries **no `Cargo.lock` change** — the advisory is
  pre-existing and unrelated to ADR-0022. Not fixed here: bumping
  `bollard`/`testcontainers` is a dependency-management change outside this
  ADR's scope, and doing it without its own review risks a second,
  unreviewed change riding along with this one.

`just ci`'s recorded wall time for `test-rust` alone was 1278.229s (~21
minutes) on this host.

## Commit 2 — Dockerfile

`frontends/apps/dashboard/app/healthz/route.ts` did not exist before this
branch — there was no health path in the dashboard app at all, so the
brief's instruction to "find it; do not assume `/healthz`" turned up nothing
to find. Added, mirroring `frontends/apps/checkout/app/healthz/route.ts`
exactly (same no-dependency, `force-dynamic`, `200 ok\n` shape).

`frontends/Dockerfile`'s `runner` stage gained `USER node`, `HOME`/
`XDG_CACHE_HOME` at `/tmp`, and a `HEALTHCHECK` against that path — parity
with the `checkout` stage, as the brief asked.

Observed, real terminal output (`DOCKER_HOST=unix:///run/user/1000/docker.sock`,
Docker 29.7.2, rootless):

```
$ docker build -f frontends/Dockerfile --target runner -t vpay-dashboard-probe .
[...]
#15 naming to docker.io/library/vpay-dashboard-probe:latest done

$ docker run --rm --name vpay-dashboard-probe-run --read-only --tmpfs /tmp:rw,size=64m \
    --user 1000:1000 -p 18734:3000 -d vpay-dashboard-probe
$ docker logs vpay-dashboard-probe-run
   ▲ Next.js 15.5.25
   - Local:        http://localhost:3000
   - Network:      http://0.0.0.0:3000
 ✓ Starting...
 ✓ Ready in 151ms

$ docker ps -a --filter name=vpay-dashboard-probe-run --format "table {{.Names}}\t{{.Status}}"
NAMES                      STATUS
vpay-dashboard-probe-run   Up 31 seconds (healthy)

$ curl -sS -o /dev/null -w "external HTTP %{http_code}\n" http://127.0.0.1:18734/healthz
external HTTP 200

$ docker exec vpay-dashboard-probe-run touch /app/should-fail; echo "exit=$?"
touch: /app/should-fail: Read-only file system
exit=1

$ docker exec vpay-dashboard-probe-run touch /tmp/should-succeed; echo "exit=$?"
exit=0

$ docker exec vpay-dashboard-probe-run id
uid=1000(node) gid=1000(node) groups=1000(node)
```

### A real defect found and fixed by this observation, not reasoned about

The first build (before `HOSTNAME=0.0.0.0`/`PORT=3000` were added to the
runner stage's `ENV`) started and answered on the **mapped** port
(`docker run -p` worked) but refused every connection to `127.0.0.1` from
**inside** the container — exactly what the `HEALTHCHECK` and a kubelet's
own probe both dial:

```
$ docker exec vpay-dashboard-probe-run node -e "fetch('http://127.0.0.1:3000/healthz')..."
ERR TypeError: fetch failed
  ... Error: connect ECONNREFUSED 127.0.0.1:3000

$ docker exec vpay-dashboard-probe-run cat /etc/hosts
127.0.0.1  localhost
172.17.0.5 78d5664a36c7
$ docker exec vpay-dashboard-probe-run printenv | grep HOSTNAME
HOSTNAME=78d5664a36c7
```

Docker itself injects `HOSTNAME=<container id>` into every container, and
Next's standalone server binds to whatever `HOSTNAME` resolves to — here,
the container's bridge IP (`172.17.0.5`), not `127.0.0.1`. External access
still worked because Docker's port-publish path reaches that same bridge
interface; the internal probe did not. Explicitly setting `HOSTNAME=0.0.0.0`
in the runner stage's `ENV` (matching what the `checkout` stage already
does, for the same unstated reason) fixed it — confirmed by rebuilding and
re-running the exact sequence above, this time green.

**Conclusion: guard 12 (`dashboard-not-templated`) is safe to retire.** The
image starts, runs non-root, refuses writes outside `/tmp`, and answers its
health path — under `--read-only --tmpfs /tmp --user 1000:1000`, exactly the
constraint a Kubernetes `readOnlyRootFilesystem: true` pod imposes. Commit 3
retires it and templates a Deployment.

## Commit 3 — Helm

```
$ just helm-check
```

```
==> helm lint (defaults, then ci/values-full.yaml, then ci/values-route.yaml)
1 chart(s) linted, 0 chart(s) failed   (x3)
==> helm template
==> template guards (each must FAIL, by name)
    guard "checkout-not-templated-by-default" fired
    guard "checkout-templated-when-enabled" fired
    guard "connection-budget" fired
    guard "dashboard-public-origin" fired
    [... 16 more, all firing by name ...]
    22 guards, all fired by name (22 expected)
==> checkout page: absent by default, present when enabled
    default: no checkout object; ci/values-full.yaml: Deployment + Service + Ingress
==> management tier and dashboard: absent by default, present when enabled
    default: no -management or -dashboard object, no HPA; ci/values-full.yaml: both Deployments + Services, HPA
==> ingress rate limit
    /v1 limit-rps=20, /v1/oauth/token limit-rps=5 (tighter, as intended)
    /provider limit-rps=10 (no looser than /v1's, on the unauthenticated route)
==> httproute rate limit
    /v1/oauth/token carries an ExtensionRef filter (the controller's own rate-limit object)
==> httproute renders nothing without the Gateway API CRDs
    no HTTPRoute without --api-versions gateway.networking.k8s.io/v1
==> httproute routes the rail callback
    /provider is a rule on the rendered HTTPRoute
==> kubeconform (downloads schemas — needs network)
Summary: 49 resources found in 3 files - Valid: 49, Invalid: 0, Errors: 0, Skipped: 0
helm-check: ok — lint, 3 renders, 22 guards, rate limit (both paths), rail callback (both paths), kubeconform. No cluster was involved.
```

Backward compatibility, checked directly: `helm template vpay
deploy/helm/vpay` with no overrides renders exactly two Deployments
(`vpay-server`, `vpay-worker`) and no `HorizontalPodAutoscaler` — the same
shape as before this ADR. `management.enabled`, `dashboard.enabled` and
`server.autoscaling.enabled` all default to `false`.

The `connection-budget` guard fires in both directions, checked by hand
beyond the `ci/guards/` fixture:

```
$ helm template vpay deploy/helm/vpay --set server.autoscaling.enabled=true
Error: ... vpay chart guard "connection-budget": server.autoscaling.enabled is true
but database.maxConnections is not set (0). ...

$ helm template vpay deploy/helm/vpay --set server.autoscaling.enabled=true \
    --set database.maxConnections=50 --set server.autoscaling.maxReplicas=10
Error: ... vpay chart guard "connection-budget": server.autoscaling.maxReplicas (10)
+ management.replicaCount (0, counted only while management.enabled) + worker.replicaCount (1),
times 10 connections each, is 110 — more than database.maxConnections (50) minus
database.reservedConnections (20) = 30. ...

$ helm template vpay deploy/helm/vpay --set server.autoscaling.enabled=true \
    --set database.maxConnections=200
# renders cleanly
```

The per-workload overlay merge (`vpay.overlayWithSurfaces`) was checked
against an operator overlay carrying its own `deployment:` block and
`merchant_clients`, to confirm the merge is a deep merge and not a
string-concatenation hack: rendering `configmap-overlay-split.yaml` with
`management.enabled: true` and a `config.overlay` setting `deployment.name`/
`.livemode`/`.public_base_url` plus a `merchant_clients` entry produced two
ConfigMaps, each carrying every one of those fields unchanged plus its own
`deployment.surfaces` (`[business]` on `-server`'s, `[management]` on
`-management`'s) — confirming `-server` and `-management` diverge only in
that one key, not in the rest of the operator's configuration.

```
cargo test --doc -p vpay-db
```

8 passed; 0 failed (confirms the `pool.rs` doc-comment addition, pairing
`MAX_CONNECTIONS` with the `connection-budget` guard, still compiles).

```
cargo check --workspace --all-targets
```

Clean, after the Helm/docs changes above.

## What was declined, and why

- **`server.autoscaling.behavior.scaleDown.stabilizationWindowSeconds`
  (60s, above the 35s `terminationGracePeriodSeconds`) has no dedicated
  `_validate.tpl` guard.** The ADR calls it "required, not optional"; this
  branch enforces it only through the default value and documentation. The
  brief's own "three things `just helm-check` will fail you on" section
  scoped the `expected_guards` array change to exactly `+connection-budget,
-dashboard-not-templated`, and adding a fourth guard name was judged
  outside that scope given the remaining time budget. Flagged here rather
  than silently — a maintainer who wants it enforced should add a guard
  comparing it against `terminationGracePeriodSeconds` the way
  `grace-period` already does for the drain deadline itself.
- **README.md's per-key `### Values` tables were not exhaustively updated**
  for every new `management.*`/`server.autoscaling.*`/`dashboard.*` field —
  the object table and guard table (what the brief named explicitly) are.
- **The dashboard's own OAuth wiring** (`dashboard.clientId`/`.redirectUri`/
  `.scope`) is templated as plain, unguarded values. This is a deliberate
  reading of the app's own fail-closed design (a missing value renders a
  "not configured" `/login` page rather than crashing), not an oversight —
  see `values.schema.json`'s `dashboard` description — but it means those
  three values need to be wired up before a dashboard sign-in actually
  works, and nothing here proves they are.
- **No Ingress rule for `/dash/v1`.** The ADR's §5 names only an HTTPRoute
  rule; no Ingress-side equivalent was added, since it was not asked for and
  the dashboard's own BFF reaches `/dash/v1` over the in-cluster Service
  directly rather than through public routing.
- **The companion `vaam-apps/vpay-skills` PR** the ADR's "Documentation and
  status" section asks for was not opened. This branch's own worktree has no
  access to that repository; flagged here per the maintainer's standing
  parity rule rather than silently skipped.

## What this does not prove

No Kubernetes Pod has run any of this. `just helm-check`'s own summary line
says so ("No cluster was involved") and this ADR does not change that —
every probe threshold, every resource number and the two NetworkPolicies are
reasoned from the manifests and from Kubernetes' documented behaviour, not
observed. The Docker-level read-only observation in Commit 2 is real; it is
not a substitute for a Pod, and the ADR's own verification checklist names
that distinction explicitly.

---

## 2026-09-16, review pass (reviewer A) — mutation results and two fixes

Added by the adversarial review of the four commits above, in the same
worktree. Everything in this section is a run, not a reading.

### Mutation testing the surface gate

Each mutation was applied to the committed source, the suite run, then the
file restored from a byte-for-byte backup. The point is to show the new
tests fail when the thing they claim to pin is broken.

| Mutation                                                                | Result                                                                                                                                            |
| ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| Baseline, unmutated                                                     | 4 passed                                                                                                                                          |
| `surfaces.business.then_some(..)` → `true.then_some(..)` (gate deleted) | **FAIL** — `management_only_unmounts_v1_and_keeps_dash_v1`: "`/v1/payment_intents` must 404 … got 401"                                            |
| `surfaces.business` → `!surfaces.business` (gate inverted)              | **FAIL** — 3 of 4, including the backward-compatibility test: "`/v1/payment_intents` must answer … when `deployment.surfaces` is absent; got 404" |
| `dash_is_configured = surfaces.management && ..` → `true && ..`         | **FAIL** — `business_only_unmounts_dash_v1_and_keeps_v1`: "`/dash/v1/payment_intents` must 404 … got 401"                                         |
| `enabled_surfaces` returns `ALL` for an empty list                      | **FAIL** — `deployment_surfaces_empty_is_a_boot_error` **and** `empty_surfaces_is_refused_before_a_router_is_ever_built`                          |
| `Config::validate_all`'s `self.deployment.enabled_surfaces()?;` deleted | **PASSED — nothing caught it.** 490/490 green in `vpay-config` + `vpay-api`. See the fix below.                                                   |

### Fix 1 — the boot-refusal wiring was pinned by no test

`Deployment::enabled_surfaces` had unit tests; nothing asserted that
`Config::validate_all` **calls** it, so the "empty `surfaces` exits 78 at
boot" claim rested on a line the suite did not read.
`validate_all_refuses_an_empty_or_unknown_deployment_surfaces`
(`vpay-config/src/config.rs`) now goes through the real entry point for both
refusable shapes and both legal ones. Re-running the same mutation against
it: **FAIL** — "validate_all must refuse an empty deployment.surfaces".

### The chart's overlay, fed to the real config parser

`vpay.overlayWithSurfaces` is a mechanism this ADR invented, and a
key-spelling mismatch there renders fine and fails at pod boot. So the
rendered ConfigMaps were extracted from `helm template … ci/values-full.yaml`
and handed to `vpay_config::Config::load_with_env` itself, as the profile
overlay beside `config/application.yml`:

```
PROFILE server:      surfaces=Some(["business"])   resolved=EnabledSurfaces { business: true,  management: false }
PROFILE management:  surfaces=Some(["management"]) resolved=EnabledSurfaces { business: false, management: true  }
PROFILE nonexistent: surfaces=None                 resolved=EnabledSurfaces { business: true,  management: true  }
PROFILE empty:       LOAD ERROR: deployment.surfaces is present and empty; …
PROFILE typo:        LOAD ERROR: deployment.surfaces contains an unknown value "businesss"; …
```

(`livemode` was flipped to `false` in the extracted copies only because the
repo's dev base config points at plaintext rail hosts; the `surfaces` key —
the thing under test — is verbatim from the chart.) The deep merge also
preserved the operator's own `deployment.name`, `livemode` and
`public_base_url` in both tiers, and with `management.enabled: false` the
`-server` Deployment still mounts `<release>-config-overlay` exactly as it
did before this ADR.

### `connection-budget`, both directions, at the boundary

Rendered, not read — `helm template` per row:

| `maxReplicas` + `management` (enabled) + `worker`, ×10 | budget (`maxConnections − reserved`) | result    |
| ------------------------------------------------------ | ------------------------------------ | --------- |
| (6 + 2 + 2) × 10 = 100                                 | 120 − 20 = **100**                   | passes    |
| (6 + 2 + 2) × 10 = 100                                 | 119 − 20 = 99                        | **fires** |
| (6 + 0 + 2) × 10 = 80 (`management.enabled: false`)    | 100 − 20 = **80**                    | passes    |
| (6 + 0 + 2) × 10 = 80                                  | 99 − 20 = 79                         | **fires** |
| any                                                    | `maxConnections` unset (`0`)         | **fires** |
| any                                                    | `reserved` > `maxConnections`        | **fires** |

Exactly at the budget passes and one over fires, which is ADR §3's `>`. The
`10` still matches `vpay_db::pool::MAX_CONNECTIONS`.

### Fix 2 — `networkPolicy.managementIngress.podSelector` was default-allow

An empty `podSelector` in a NetworkPolicy peer matches **every pod in the
namespace**. With `networkPolicy.enabled` and `management.enabled` both true
and the chart's `{}` default, the rendered `-management` policy was:

```yaml
from:
  - podSelector:
      matchLabels: {}
```

— the whole namespace admitted to `/dash/v1`, under a template comment that
said it "admits only the pods `networkPolicy.managementIngress.podSelector`
names", and with `ci/values-full.yaml` deliberately rendering that shape.
This is the concrete form ADR §5 asks for, so a default that isolates
nothing is the defect, not a convenience. It is also the exact class the
chart already refuses elsewhere (`networkpolicy-database`,
`rails-egress-except`).

Added guard 23, `networkpolicy-management-ingress`, with its
`ci/guards/` case, the `justfile` expected-set entry, the README row and the
count prose (twenty-two → twenty-three). `ci/values-full.yaml` now names the
dashboard's own labels, and renders:

```yaml
from:
  - podSelector:
      matchLabels:
        app.kubernetes.io/component: dashboard
        app.kubernetes.io/name: vpay
```

### Rendered-YAML checks (not template source)

- `-server` Deployment emits **no** `spec.replicas` when
  `server.autoscaling.enabled`; the HPA carries `minReplicas: 3`,
  `maxReplicas: 6`, `scaleDown.stabilizationWindowSeconds: 60` (above the
  35s `terminationGracePeriodSeconds`).
- `pdb-minavailable` compares against `server.autoscaling.minReplicas` when
  the HPA is on: `minAvailable: 1` with `minReplicas: 1` fires, and the
  rendered pair is `minAvailable: 2` against `minReplicas: 3`.

### The dashboard image, rebuilt and re-run read-only

Independently reproduced, not taken on report:

```
docker build -f frontends/Dockerfile --target runner -t reva-dashboard-probe .   # exit 0
docker run -d --read-only --tmpfs /tmp:rw,size=64m --user 1000:1000 -p 13000:3000 …
  health=starting → healthy in ~4s
  docker exec … id            → uid=1000(node) gid=1000(node)
  curl http://127.0.0.1:13000/healthz → HTTP/1.1 200 OK
  touch /app/PROBE            → "Read-only file system", exit=1
  touch /tmp/PROBE            → writable
  logs                        → "✓ Ready in 155ms"
```

Guard 12's retirement is carried by this, not by a reading of the
Dockerfile.

### What the review did **not** check

- No Kubernetes Pod ran, unchanged from the section above.
- `/v1/browser`, `/provider` and `/v1/oauth` are not walked by the new
  surface tests — only `V1_ROUTES` and `DASH_ROUTES` are. The first two sit
  in the same `match` arm as `/v1` (so no separate gate can drift), but
  `/v1/oauth` really is mounted on a management-only process by design, and
  nothing asserts that either way.
- The staff sign-in routes were traced by reading `staff::routes()` (all
  eight, including `/oauth/authorize` and `/oauth/token`, register **inside**
  the `/dash/v1` nest) rather than by an HTTP assertion.
