# Verification log — 2026-09-11 and 2026-09-10 (exp57, exp51, exp45, exp46)

## 2026-09-11 — `claude/exp57-docs-split`, the documentation split

_This entry is above the archive below rather than inside it: the note on that
block says its text is the original and unedited, and appending to it would
make the note false._

Last verified: 2026-09-11, on branch `claude/exp57-docs-split` at `722e579`,
the head of the documentation split — eleven documents became an overview plus
a directory. **No capability moved and no Rust behaviour changed**; the only
non-documentation change is `.xtask`'s retired-name allowlist and the justfile
comment describing it.

**`just ci` exit 0**, exit code read from a file rather than a banner:
`fmt-check` (both halves); `clippy` `-D warnings`; `verify`, all twelve gates
(`verify-links` **1 450 links in 309 tracked files**, `verify-status` 1
declared unimplemented item, `verify-errors` 19 error types / 16 `#[from]`
variants, `verify-sdk-parity` 463 proving tests / 33 dated gaps / 32 methods
across 35 rows, `check-schema` 26 declarations, `verify-serde` 90 types / 16
exemptions, `verify-repositories` 4 implementations / 83 source files outside,
`verify-toolchain` 1.98.0, `verify-migrations` 42 files); `test-rust`
**1 748 tests run, 1 748 passed, 0 skipped** across **46** binaries against a
real Postgres and real WireMock rails (1 237 s, one slow case at 64 s);
`test-doc` **113 passed, 1 ignored** (`sdks/rust`'s README block,
pre-existing); `verify-ignored` **0 ignored (expected 0), 46 binaries
(expected 46), 1 748 total (minimum 1 080)**; `lint-web`; `test-web` **1 386
tests**, every suite reporting passes only (`@vpay/checkout` 507, `@vpay/dashboard` 243,
`@vaam-apps/vpay-sdk` 210, `@vaam-apps/vpay-stripe-js` 146, `examples/shop`
108, `ui` 97, `config` 63, `tokens` 8, `api-client` 4); `deny` — advisories,
bans, licenses, sources all ok.

**This branch adds exactly two tests**, both in `xtask`, both pinning the
allowlist entry the split forced. That is measured rather than subtracted:
master's `.xtask/src/main.rs` was checked out over this branch's and
`cargo test -p xtask` answered **232**, against **234** here. **The
workspace total on `origin/master` was not re-measured** — a full rebuild of a
tree this branch has already built over, to derive a number arithmetic would
give, is not evidence worth its cost; 1 748 is what this head printed.

**The first `just ci` on this branch exited 1, and the gate was right.**
`verify-npm-scope` reported seven occurrences of a retired `@vpay/*` package
name in `docs/status/gates.md`: its allowlist exempts `docs/status.md` because
the dated entry recording the rename has to spell what it renamed, the split
moved that entry, and the list is prefix-matched. The fix adds `docs/status/`
as a fifth entry; deleting it again reproduces the same seven occurrences, and
`the_same_retired_name_under_the_docs_status_archive_passes` fails.

**`check-schema` did not run under the version this repository pins.**
`justfile`'s `cratestack_version` is `0.12.0`; the CrateStack CLI on this
machine's `PATH` is **0.11.1**, and the recipe says so out loud and runs the
check in full against the 0.11.1 grammar — 26 model/enum declarations,
datasource present. That is a property of this machine, not of this tree.

**Not run for this entry:** `just docs-check-citations` (network and a token),
`just helm-check` (network; no chart change), `just test-e2e` and
`just demo-walk` (nothing on this branch touches the compose stack, a browser,
or any code either of them runs). **And nothing in `just ci` reads a
sentence** — a green gate says the documents parse and their links resolve, not
that a page still says what it said before it moved. The evidence for that is
the whitespace-tolerant diff of each original against its new set of pages,
recorded in each commit, and the anchor sweep that found four stale fragments
across the tree.

---

## Archived from `docs/status.md`

_Archived from [docs/status.md](../../status.md) on 2026-09-11 by exp57, which split a 6 151-line page into pages a person can read. **The text below is the original, unedited** — every dated measurement, every struck-through claim and every correction is here as it was written; only the relative links gained a `../../` because the file moved two directories down._

_It still says "above" and "below" where it once pointed at another part of the same page. Those targets are on sibling pages now, and [README.md](../README.md) is the index of them._

Three entries, **in the order `docs/status.md` carried them, which is not date order**:
`claude/exp51-demo-tenant` (2026-09-11), the header of
`claude/exp45-worker-pool-bound` (2026-09-10), and
`claude/exp46-customer-address` (2026-09-11). On master the exp46 entry sits between
exp45's opening paragraph and exp45's own `helm-check` and "what the draft's gate did
not cover" paragraphs, so exp45's `just ci` paragraph is not in the page at all. That
interleaving is reproduced here rather than repaired: reordering it would change the
record, and the missing paragraph is a gap that predates this split.

Last verified: 2026-09-11, on branch `claude/exp51-demo-tenant` at the head of
the **sabotage review** of the demo-tenancy fix (base `6b1b7d8`; the
implementation's five commits are kept and the review adds one, and
[plans/exp51-demo-tenant-notes/opus-review.md](../../plans/exp51-demo-tenant-notes/opus-review.md)
says what it found).

**This entry leads with `just test-e2e`, and that is the point of it.** That
recipe is not part of `just ci` and never has been. The branch's `just ci` was
clean, its browser specs were not, and its notes recorded neither — §6 of
them, "Gates, recipe by recipe … filled in below as each was run", is empty,
and the run that would have filled it had already failed.

**`just test-e2e` exit 0 on the review head**, exit code read from a file:
**22 Cypress tests, 22 passing, 0 failing, 0 skipped** across four specs and
two `cypress run`s — `checkout.cy.ts` 1, `dashboard.cy.ts` 11,
`shop-hosted.cy.ts` 4 in the default run and `shop-embedded.cy.ts` 6 in the
framed one — against a compose stack of the review's own (project `exp51j`:
server :19900, dashboard :14900, shop :14902, checkout :14901, orange stub
:19902), real Postgres, real `vpay-worker`, WireMock rails.

**On the branch as delivered the same command was 20 of 22** —
`dashboard.cy.ts` 9/11, measured locally and identical to what the
implementation's own two runs had already seen — and CI's `e2e (compose)` job
was worse still: `checkout.cy.ts` 0/1 and `dashboard.cy.ts` 8/11 on run
34555068739, because the workflow carried its own copy of the recipe's
environment and that copy still named `demo-merchant`. Two independent causes,
plus a third behind the override the runbook tells a reader to use. All three
are measured in the review notes.

**The override still fails, and now fails in the right place.**
`just demo_dashboard_merchant=demo-merchant-tenant … test-e2e` is the
arrangement that variable exists to prevent. On the review head the staff
member signs in (four legs green), the shop takes a real payment, and the
three cases that read a payment out of `/dash/v1` fail. Before the review it
failed at `/login` instead — `test-e2e`'s `demo-staff` sub-invocation did not
forward the variable, so the dashboard and the only staff member named
different tenants — which looks identical from outside and proves nothing.

**`just ci` exit 0 on the review head**, exit code read from a file:
`fmt-check`; `clippy` `-D warnings`; `verify`, all twelve gates
(`verify-links` **1067 links in 212 tracked files**, `verify-status` 1 declared
unimplemented item, `verify-errors` 19 error types / 16 `#[from]` variants,
`verify-sdk-parity` 455 proving tests / 33 dated gaps, `check-schema` at
cratestack 0.11.1, `verify-serde` 87 types / 16 exemptions,
`verify-repositories` 4 implementations, `verify-toolchain` 1.98.0,
`verify-migrations` 41 files); `test-rust` **1696 tests run, 1696 passed, 0
skipped** across **46** binaries against a real Postgres and real WireMock
rails (1239 s); `test-doc` **111 passed, 1 ignored** (`sdks/rust`'s README
block, pre-existing); `verify-ignored` **0 ignored (expected 0), 46 binaries
(expected 46), 1696 total (minimum 1080)**; `lint-web`; `test-web` (0 skipped;
`@vpay/checkout` 507 in 24 files, `@vpay/dashboard` 182 in 21, `examples/shop`
102 in 12, `@vaam-apps/vpay-sdk` 208 in 9, `@vaam-apps/vpay-stripe-js` 146 in
9); `deny`. **No Rust, schema or migration changed on this branch**, so
1696 is the base's number and not this change's.

**Two earlier `just ci` runs on this same head exited 100, and both were this
host and not this branch.** Each died in `test-rust` on
`failed to create a container` from `testcontainers` — the first two because
`DOCKER_HOST` was unset in the runner's environment and this machine's Docker
is rootless (`unix:///run/user/1000/docker.sock`; the socket at
`/var/run/docker.sock` belongs to root and refuses the connection), the third
on one container-start under the load of ~1300 container-backed tests
alongside another agent's build. The green run sets `DOCKER_HOST` and
`NEXTEST_TEST_THREADS=4`. Neither changes what is tested, and both are
recorded here rather than dropped: a green obtained by reruns is worth exactly
what its reruns are explained by.

**Not run for this entry:** `just docs-check-citations` (network and a token),
`just helm-check` (network; no chart change), `just demo-walk` (untouched by
this branch; `docs/runbooks/demo.md` §6 states where its payments land and
that this dashboard does not show them).

Last verified: 2026-09-10, on branch `claude/exp45-worker-pool-bound` at the
head of the **sabotage review** of issue #63's worker-concurrency bound (base
`2c5ef8b`; the haiku draft's five commits are kept and the review adds six,
and
[plans/exp45-worker-pool-bound-notes/opus-review.md](../../plans/exp45-worker-pool-bound-notes/opus-review.md)
says what each one is for).

Last verified: 2026-09-11, on branch `claude/exp46-customer-address` at the
head of the **sabotage review** of issues #67/#68/#96 item 2 — the Customer
address and the erasure (rebased onto `5e3a004`, which is #108's `0040` and
#109's repo-wide prettier gate; the draft's six commits are kept and the
review adds nine (plus this row), and
[plans/exp46-customer-address-notes/opus-review.md](../../plans/exp46-customer-address-notes/opus-review.md)
says what each one is for and what it was found by).

**`just ci` exit 0 on the review head**, recipe by recipe, exit code read from
a file — run twice, at `603fc7a` (1180 s) and again at `c1f2286` (1110 s),
because the second added this row and a doc must not be described by a gate
that ran before it. The branch head is **one docs-only commit later than
`c1f2286`** — this paragraph, which cannot describe the run that precedes it
without saying so. `fmt-check`, `clippy` and `verify`'s twelve gates were
re-run on that head; `test-rust` was not, and nothing in that commit is
compiled by anything. The numbers below are `c1f2286`'s: `fmt-check` (both halves — `cargo fmt` and
`pnpm exec prettier --check .`, the second a gate only since #109);
`clippy` `-D warnings`; `verify`, all twelve gates (`verify-links` **1069
links in 210 tracked files**, `verify-status` 1 declared unimplemented item,
`verify-errors` 19 error types / 16 `#[from]` variants, `verify-sdk-parity`
**459 proving tests / 33 dated gaps / 32 methods across 35 rows**,
`check-schema` 26 declarations at cratestack 0.12.0, `verify-serde` 89 types /
16 exemptions, `verify-repositories` 4 implementations, `verify-toolchain`
1.98.0, `verify-migrations` **41 files**); `test-rust` **1698 tests run, 1698
passed, 0 skipped** across **46** binaries against a real Postgres and real
WireMock rails (1180 s); `test-doc` **112 passed, 1 ignored** (`sdks/rust`'s
README block, pre-existing); `verify-ignored` **0 ignored (expected 0), 46
binaries (expected 46), 1698 total (floor 1080)**; `lint-web`; `test-web` (0
skipped; `@vpay/checkout` 507 in 24 files, `@vpay/dashboard` 182 in 21,
`@vaam-apps/vpay-sdk` 210 in 9, `@vaam-apps/vpay-stripe-js` 146 in 9,
`examples/shop` 102 in 12, `ui` 74 in 18, and the three small packages);
`deny` — advisories, bans, licenses, sources all ok.

**Three runs before that one failed, and all three are recorded rather than
dropped**, because two of them were the environment and one was a real gate
catching a real mistake:

1. `fmt-check-web` on `docs/plans/exp46-customer-address-notes/opus.md` — the
   branch's notes predate #109's prettier gate. A real failure of a real gate;
   fixed by running the formatter.
2. `vpay-db config_reconcile::…` — "failed to create a container: Timeout
   error", twice, on a machine running two other agents' suites and fourteen
   containers. Not this branch: no code path in it touches
   `config_reconcile`, and the run that followed a cleanup of two orphaned
   scratch containers passed it.
3. `vpay-sdk::token_exchange
a_second_concurrent_401_does_not_discard_the_token_the_first_one_just_fetched`
   — a wiremock verification of a concurrency window. That file is
   **untouched** by this branch (`git diff origin/master..HEAD` is empty for
   it) and the binary passed 17/17 three times in a row immediately
   afterwards, and again in the green run. A load flake, and it is worth
   re-reading if it recurs on an idle machine.

**1698 total, and master's own number was not re-measured on this head** —
`cargo nextest list` against `5e3a004` needs a full rebuild of a tree this
branch has already rebuilt over, and a subtracted number is not a measured
one. What _is_ countable is what this branch adds: **seven new `#[test]` /
`#[tokio::test]` functions** over `origin/master..HEAD`, of which one —
`an_erasure_mid_ladder_redelivers_the_redacted_body_instead_of_dead_lettering`
— is the review's, and the review's other additions are new assertions inside
existing cases (the NULL half of the marker loop, and the two `failure_raw`
places the scanner's `before` now names). The 46 binaries are master's 46.

**`just helm-check` was NOT run on this head**, and nothing on this branch
touches the chart — no template, no value, no guard. The last recorded run is
exp45's, below.

**`just helm-check` exit 0** on the same head — not part of `just ci`, and run
separately for that reason: `19 guards, all fired by name (19 expected)`,
`kubeconform` `23 resources found in 2 files - Valid: 23, Invalid: 0, Errors:
0, Skipped: 0`. The nineteenth guard is `worker-concurrency-pool`, new here,
and it found that `ci/values-full.yaml` had been shipping
`worker.concurrency: 8` — a value this binary refuses.

**What the draft's own gate did not cover, and it is why this branch exists:**
`6cfd78e` fails `test-rust` (`1328/1666 tests run: 1327 passed, 1 failed`),
because its new "boots" case asserted on the subprocess's _stderr_ while
worker logs go to stdout. That case was the draft's stated proof, and the
tests were never run. Beneath it the rule itself had never been measured:
`MAX_CONNECTIONS / 2` is the ceiling and the cliff is at `MAX_CONNECTIONS`,
which the new integration case and the probe in the review notes now pin.
**Not run here:** `just docs-check-citations` (network + token), `just
test-e2e`, `just demo-walk` — nothing on this branch touches the compose stack
or a browser.

<!-- Ported during exp57's rebase: these entries landed on master (#128, #130) while this branch was out, and belong on this page rather than in the index. -->

**#122 — `verify-sdk-parity` proves a name exists, not that the SDK sends the
field — attempted 2026-09-11, reverted the same day, still open.**
`claude/b1b-test-integrity`'s draft added a sixth check: for a row whose
capability text contains "body", "path" or "exact", the named test's own
_source lines_ (not its assertions' targets) must contain the substring
`.body` or `.path`. Reviewed against the issue's own decisive check —
delete `latitude_microdeg` from the Node SDK's `addressBody`, run
`cargo xtask verify-sdk-parity`, the mutation must be caught — it is not:
`verify-sdk-parity`'s output is **byte-for-byte identical with and without
the mutation** (`diff` of the two runs is empty), because the check reads the
_test's_ text and the mutation only changes the _SDK's_. Worse, the gate as
merged already fails on the branch's own, unmutated head: **exit 1 before any
mutation**, with 33 violations against tests that assert real request shapes
through helpers the text-scan does not recognise (`the_two_attempts`,
`req.formData()`, deep-equality on a parsed object, and others) rather than a
literal `.body`/`.path` substring — landing this would have broken `just
verify` for the whole tree over false positives, on top of not catching the
mutation the issue is named after. Reverted: `.xtask/src/main.rs` is back to
`face3da`'s version (pre-branch); `cargo xtask verify-sdk-parity` again exits
0, `ok — 463 proving test(s) … all exist`, on the same mutation, which is
exactly issue #122's original finding, re-confirmed rather than assumed.
**#122 stays open.** The issue names three options and says picking one is a
maintainer call; this review's finding is that option 2 (a syntactic
body/path check) does not survive contact with the mutation it exists to
catch, and probably cannot be made to without becoming a partial
re-implementation of a source parser — so the honest remaining choices are
option 1 (say what the gate actually proves) or option 3 (run the SDK suites
instead of inspecting them).

**2026-09-11 (issues #103, #125): CI gate defects fixed.** (Branch `claude/b1a-ci-gates`)

- **Issue #103**: `audit-web` recipe ran at `--audit-level=high` and was not part of `just ci`, so it missed the moderate advisory GHSA-82fw-gwwq-j7x9 for two days. Fixed: (1) changed the level to `--audit-level=moderate` in both audit invocations; (2) added `audit-web` to the `ci` recipe; (3) updated the error message to say "moderate, high or critical advisory" instead of just "high or critical". CI's workflow already runs `just audit-web` as its own step in the `web` job, so the recipe was wired to CI already; that step's name and its comment still said "high and critical fail" and "`--audit-level=high`" after this change — fixed in the same commit as this note (`.github/workflows/ci.yml`).

  **Corrected by review, same day.** The draft's own report said it did not run `just audit-web` locally, then concluded "the advisory GHSA-82fw-gwwq-j7x9 is now caught at the moderate level" anyway — an unverified claim from an agent that shares this host and was told not to run `just ci`, not `just audit-web` itself. The review ran `just audit-web` for real: **`No known vulnerabilities found`** at both `--prod` and whole-workspace, `--audit-level=moderate`. GHSA-82fw-gwwq-j7x9 is not "caught" by this change — it simply is not present any more, cleared on 2026-09-10 by the unrelated `vitest` `^3.2.7` → `^4.1.11` bump (commit `9ef43cc`, see the `just audit-web` row below). So the literal claim in the draft's own commit message is false on this tree today, and the level change cannot be demonstrated against the named advisory because the named advisory is gone. The mechanism the level change fixes was demonstrated instead, bounded and reproducing the advisory's own numbers: a throwaway `package.json` depending on `vitest@3.2.7` alone (outside the pnpm workspace, `--ignore-workspace`), audited both ways —
  `pnpm audit --audit-level=high` exits **0** over 2 moderate findings (GHSA-82fw-gwwq-j7x9, `vitest` and `@vitest/mocker`); `pnpm audit --audit-level=moderate` exits **1** naming both. That is the class of defect issue #103 reported, reproduced and shown fixed; the specific advisory the issue cites just isn't there to catch any more. Also verified: `just -n ci`'s dry-run expansion shows `audit-web`'s recipe body between `test-web` and `cargo deny check`, confirming it is actually in the `ci` dependency chain, not merely mentioned in the recipe list. And the tree today has **zero** moderate/high/critical advisories at either `--prod` or whole-workspace, so the stricter ceiling blocks nothing on this PR — the next moderate advisory in the dependency tree is the first real test of it. Issue #103's decision also asked for "a documented allowlist mechanism for an accepted advisory"; that already exists (`pnpm.overrides` plus `package.json`'s `"//pnpm"` block, in place since the 2026-09-03 audit-clearing pass below) and was not exercised here because there is currently nothing to allow.

- **Issue #125**: `just test-e2e` tore down only at the **end**, so a re-run against a surviving volume failed on the first leg (sign-in) because `demo-staff` kept an existing member and `dashboard.cy.ts` changes their password in leg 4. Fixed: added `docker compose {{demo_compose}} down -v` at the **start** of the recipe, right after `set -e`, to ensure each run begins from a clean volume. This also ensures test isolation on a developer machine.

  **Checked by review.** `VPAY_DEMO_PROJECT` is exported before the new `down -v` call, and `compose.demo.yml` — last in `demo_compose`'s `-f` list, so its `name:` wins — reads exactly that variable; every other `down`/`up`/`logs` call in the recipe resolves through the same `{{demo_compose}}` and the same exported variable, so the new `down -v` targets the same project as the rest of the recipe, not a no-op against the wrong one. The end-of-recipe teardown (`docker compose {{demo_compose}} down -v` before `exit $e2e_status`) is unchanged and still unconditional. One real cost, not a defect: `demo_project`'s default (`vpay-demo`) is the same default `just demo-up` uses for manual poking around, so running `just test-e2e` now destroys a co-resident `demo-up` session on the default project name — exactly the tradeoff issue #125's own author flagged and accepted when picking Option 1 ("the recipe may keep the volume deliberately for speed, and that tradeoff belongs to whoever set it up"). Not independently exercised end-to-end (would mean running `just test-e2e` twice back-to-back against a live Docker stack, which the OOM constraint on this host rules out); the read of the justfile is what this note is based on.

**Note**: Full `just ci` was not run per batch constraints (OOM risk with five concurrent agents); `just audit-web` and a bounded `pnpm audit` demonstration were, by the review (see above); the fixes are ready for GitHub Actions verification.

re-reading if it recurs on an idle machine. **Filed as #124 and fixed
2026-09-11 (`claude/b1b-test-integrity`, reviewed):** the test's own
`.expect(1)` counts on the two GET routes' `401` mocks were the ordering
assumption a busy scheduler is free to break, so they are gone; the
guarantee is now asserted behaviorally instead — both routes' `200` mocks
answer only `Bearer tok_fresh`, so either caller succeeding already proves
it holds the fresh token, and a `token_requests.len() == 2` check after
both calls (mount_staleness_then_freshness's own `.expect(1)`s cover the
same thing) proves the cache was not cleared a third time. Judged net
equally strong on the guarantee the test names in its own comment, weaker
only on the incidental claim that each caller's `401` was individually
observed — which was never the property #124 asked this test to hold.
Re-run by the review, not assumed: **39/39 passed**
(`cargo nextest run -p vpay-sdk --test token_exchange -E
'test(a_second_concurrent_401_does_not_discard_the_token_the_first_one_just_fetched)'`
— 30 solo at `--test-threads 16/32`, 5×`-p vpay-sdk` full-suite runs at
`--test-threads 16`, and 4 copies launched as genuinely simultaneous OS
processes for real contention, all pass), and the test is not vacuous:
reverting `invalidate_if_current` to an unconditional clear (the exact bug
#124's docstring describes) fails it immediately —
`assertion left == right failed: expected exactly two token requests …,
but got 3`.
| Customers — `/v1/customers` (`vpay_api::v1::customers`, `vpay_db::customers`, migrations `0034` and `0041`) | ✅ | **New 2026-09-06 (S4a).** `cus_…` — the merchant-owned record of a payer, five routes (`create`/`retrieve`/`update`/`list`/`del`), both SDKs, and a twelve-month retention sweep that is **built and runs**, not `NotImplemented`. [flows/customers.md](../../flows/customers.md) is the whole document; the load-bearing facts: the maintainer's three decisions of 2026-09-05 are in the schema and in tests (`phone` stored by default with no opt-in; **a phone number alone is a complete customer** — `at_least_one_identifier` requires _one_ of name/email/phone, proven in both directions against a real Postgres; retention is twelve months, `CUSTOMER_IDLE_AFTER`, deliberately not configurable because it is a promise to a payer). `phone` is canonicalised through the **same** `canonical_msisdn` the account-holder lookup uses, so vpay never holds two spellings of one payer. `DELETE` is an **erasure**, in one of two shapes (see the 2026-09-10 paragraph below; this sentence read "a **hard** delete … turned into a `409`" until then). Clearing the _last_ identifier is a `400` decided above the statement (the database's own CHECK is a `23514` → `Category::Storage` → a `503` telling a merchant to wait for a healthy database). `customer` on `POST /v1/payment_intents` **stopped being accepted-and-dropped** in the same migration and is stored and rendered; the intent object went 12 keys → 13. Proven by **23 container-backed cases** in `backends/tests/integration/tests/customers.rs` (the 44th test binary) driving the shipping router, the shipping SDK and the shipping worker loop, 6 in `postgres_smoke.rs` at the schema, 11 in `vpay-db` with no container plus 2 container-backed in `vpay-db`'s own `repositories.rs`, 6 in `vpay_api::model`, 9 in `sdks/rust` and 11 in `sdks/nodejs`. **Every number in that sentence was re-counted on 2026-09-11 by the review of the GPS half**, and every one of them had gone stale: it read "18 … 3 … 7 … 2 … 8 … 8", which were the counts on 2026-09-06 and had not moved with the three changes below it. The later sentence in this row saying "the whole customer suite at 23 container-backed cases" was the only count here that had been kept current. **✅ rather than 🟡, and the bar it is measured against:** every claim in this row fails a named test if it breaks, including the sweep's three cases (deleted / kept-because-recent / kept-because-referenced) and the confirm-path retention stamp. **Five of those cases were added by the sabotage review on 2026-09-07 and each closed a measured hole**, which is why the count moved after the row was first written: the object's key set was guarded by nothing (`the_customer_object_is_the_documented_eight_keys` — rendering the internal `last_used_at` on the wire was green across `vpay-api`, `vpay-sdk`, the integration suite and the Node SDK, and two documents named a tripwire of that name that did not exist; the count in them was also wrong, seven for eight); the list's tenancy and its **cursor** were guarded by nothing (`a_list_is_tenant_scoped_and_a_foreign_cursor_pages_nothing` — dropping the tenant filter from `list_page`'s cursor subquery passed all twelve delivered cases, and the forged cursor then pages another merchant's rows); and `POST /v1/customers`'s idempotency was guarded by nothing (`a_replayed_create_answers_the_stored_customer_and_a_reused_key_is_refused` — releasing the key instead of storing the response passed all twelve, and a retried create then mints a second `cus_…` for one payer). A fifth case, `a_sessions_customer_is_inherited_supplied_or_a_refused_contradiction`, covers the **checkout session** half, which S4a documented in four sentences and tested in none: inherited from the intent, supplied directly, refused when the two disagree, the same uniform `400` for another merchant's `cus_…`, and a customer that only a _session_ references pinned against `DELETE` — deleting the contradiction check left the entire thirty-one-case `checkout_sessions.rs` suite green. **A gap that review found and a later review the same day closed ([issue #70](https://github.com/vaam-apps/vpay/issues/70) item 1): a session's `customer` is now sent on `checkout.sessions.create` and decoded off the object, in both SDKs.** `CreateCheckoutSessionParams` had the field in neither language; only the decode half (`CheckoutSession::customer`) had been added, on 2026-09-06, which is why a first pass at this issue could describe the field as "already implemented in both SDKs" and be wrong — nothing had ever sent one. `to_form`/the request body now carry it, proven by a body-asserting test in each SDK (`create_checkout_session_sends_the_documented_body_and_decodes_the_object` and `create_checkout_session_body_matches_the_node_sdk_byte_for_byte` in `sdks/rust`; the two same-shaped tests in `sdks/nodejs/src/client.test.ts`) plus a decode test with a non-null value in each. The parity row in [sdks/parity.md](../../sdks/parity.md) is ✅/✅ rather than the dated ⛔/⛔ it carried. **Updated 2026-09-10 ([issue #66](https://github.com/vaam-apps/vpay/issues/66)): `POST /v1/customers` and `POST /v1/customers/{id}` are transactional, and each emits.** Migration `0039` adds `customer.created` and `customer.updated` to `type_is_a_documented_event` **in the same change that writes them** — migration `0023`'s lockstep rule working as intended, and the reason `0034` declined to add them. The create's event commits with the insert; the update's commits with the `UPDATE`, and the update's read is now `SELECT … FOR UPDATE`. **That lock is the part that mattered.** `metadata` is merged key-wise, so the written value is a function of the stored one, and `vpay_api::v1::customers::update`'s own doc comment said in as many words that two concurrent updates each adding one key could lose one — tolerable while nothing depended on the result being definite, and not tolerable once an event describes it. Three decisive mutations were run on 2026-09-10: removing `FOR UPDATE` fails `two_concurrent_metadata_merges_keep_both_keys_and_the_event_carries_the_committed_state` (six rounds, `{"right":"2"}` — the `left` key clobbered) **and** `a_locked_customer_read_waits_for_the_writer_and_then_sees_its_value`, which forces the interleaving deterministically at the seam rather than racing for it; dropping `customer.updated` from `0039` fails both the vocabulary case and the create/update case with a `23514`; renaming the Node parity test fails `verify-sdk-parity` naming that column. A **bodiless** update emits nothing, and a `404` writes nothing at all. Both SDK unions gained the two types, and the dated ⛔/⛔ parity row from 2026-09-06 is closed. **Updated 2026-09-10 again ([#67](https://github.com/vaam-apps/vpay/issues/67), [#68](https://github.com/vaam-apps/vpay/issues/68), [#96](https://github.com/vaam-apps/vpay/issues/96) item 2, migration `0041`): the address, and an erasure that is actually complete.** The object gained `address` — one nested object of six nullable components, eight keys → nine — with `country` validated as ISO 3166-1 alpha-2 and upper-cased on the way in, and an update **replacing** the address whole rather than merging components (`address=` clears it). Both SDKs carry it in both directions with body-asserting tests. **The erasure is the larger half and it reverses a documented trade.** `DELETE /v1/customers/{id}` no longer answers `409` and no longer emits nothing: it hard-deletes a customer nothing references and **anonymises** one an intent, a session or an invoice does — every identifier column becomes the literal `[redacted]`, `anonymized_at` is stamped, `metadata` (the merchant's data) stays, the `NO ACTION` foreign keys are untouched so the payment record survives with no payer on it, and the object renders `deleted: true` so a merchant's stored `cus_…` keeps resolving. The sweep does the same and stops skipping referenced customers, which is what had exempted every paying payer from the twelve-month promise. **The reading is what changed the design and it is worth recording:** issue #68 asks for redaction "on retained intents and sessions", and an intent has never carried a payer identifier — the surviving copies were `events.data` (every `customer.*` body ever written, in a table nothing prunes), `charges.payer_ref`/`payer_ref_masked` (reachable from a customer only through an intent) and `idempotency_keys.response_body` — and, found the same way by the **sabotage review of 2026-09-11**, `charges.failure_raw` and `refunds.failure_raw`, which are not identifier columns and hold identifiers anyway: they are the rail's message kept verbatim, and a mobile-money rail refusing a collection names the subscriber it refused it for. All five are rewritten in the erasure's own transaction. `customer.deleted` now carries the **redacted** object, which reverses what `flows/webhooks.md` and `flows/customers.md` both argued; the merchant already received the identifiers when the earlier events were delivered, and the copy vpay keeps for ever is the one the promise is about. Proven by `an_erasure_leaves_no_payer_identifier_in_any_column_of_any_table`, which scans every `text`, `varchar` and `jsonb` column `information_schema` reports — before and after — rather than the tables somebody thought of; by `an_anonymised_customer_carries_the_marker_in_every_identifier_column`, which un-marks each of **eleven** columns in turn against migration `0041`'s `anonymized_customers_carry_the_marker` — the nine text ones **twice each since 2026-09-11, once as another value and once as `NULL`**, because the `=` spelling the CHECK shipped with passed on NULL (a CHECK is violated only by FALSE) and therefore accepted the partial erasure it was written to make unrepresentable, and the two coordinate ones once each as an in-range value, since `NULL` is their legal erased state; and by the whole customer suite at **23** container-backed cases. Drift 172 → 176, measured: +5 hand-named address CHECKs, −1 because `phone_is_a_canonical_msisdn` became multi-column and left the report, and **zero** column-level lines for seven new columns. **Decisive mutations run on 2026-09-10:** dropping one column from the marker CHECK fails the postgres_smoke case naming that column; deleting the `charges` redaction statement fails the scanner; deleting the third `NOT EXISTS` from `UNREFERENCED` fails `an_invoiced_customer_is_anonymised_rather_than_deleted` with a rolled-back `23503`; deleting `checked_country` fails `a_country_that_is_not_alpha_2_is_a_400_and_not_the_databases_503`. **Extended 2026-09-11 with the GPS half of the address (the maintainer: "address in our system means both formal as well as GPS").** `address` carries `latitude_microdeg` and `longitude_microdeg` — **integers in microdegrees, never floating point**, and the unit is in the field name so no JSON float appears on this API at all. Two measurements decide the type rather than taste: `Value::from_plain_json` routes every JSON number through `Number::as_i64()` and demotes anything else to `f64`, and the money layer's own precedent is integer minor units with the scale named. It is a **deliberate divergence from Stripe**, whose address has no coordinate, stated in [api/README.md](../../api/README.md), [flows/customers.md](../../flows/customers.md) and both SDKs' types. `0041` grew the two `BIGINT` columns (edited in place — it exists only on this unmerged branch — with its manifest line updated by hand), a range CHECK per axis, and `address_coordinates_are_both_or_neither`, which is widened by `anonymized_at IS NOT NULL` so the marker CHECK is the only constraint that can fire on a row claiming to be erased. The erasure NULLs both columns rather than marking them: **there is no integer that is not a possible place**, so for those two the erasure is the absence, and the marker CHECK now states the rule per column type. A payer's coordinates are the most sensitive field on this object, and the scan that proves the erasure **cannot see them** — `scan_for` reads `text`/`varchar`/`jsonb` and these are `BIGINT` — so that third limit is stated on the scanner and the columns are asserted directly, by name, as NULL; what the scan does cover is the **rendered** copies in `events.data` and `idempotency_keys.response_body`, whose digits are in the literal list. Drift **177 → 179**, measured on a freshly migrated database: the two single-column range CHECKs, reported once each as `[safe] … is not declared` exactly as the six length bounds are, and **zero** lines for the two new columns or the multi-column pair rule. **Seven decisive mutations run on 2026-09-11**, each restored afterwards and each named with the message it produced in [plans/exp46-customer-address-notes/exp49-gps.md](../../plans/exp46-customer-address-notes/exp49-gps.md): dropping a coordinate from the marker CHECK, dropping a range CHECK, dropping the pair rule, sparing the coordinate in the event-body redaction (which the scanner catches with `{"4061777": ["events.data", "idempotency_keys.response_body"]}`), removing the API's pair refusal, making `CustomerAddress::redacted` keep the point, and removing the field from each SDK's encoder in turn. Two of them first came back as a 120 s testcontainers `StartupTimeout` on a saturated host, which is not a caught mutation and is recorded as such: both were re-run with `--retries 3` after confirming the unmutated case passes on retry. What it does **not** claim, and the flow doc lists in full: no `email` filter on the list; neither SDK has run this resource against a live vpay; **`customer.updated` has not been fanned out to a receiver in any suite** — the fan-out is type-agnostic and has been observed for five other types, which is an argument and not a measurement. This said `customer.created` too until 2026-09-11, when the review's mid-ladder erasure case drove one through the shipping fan-out and delivery handler into the WireMock receiver's journal; **no deployment has ever run the retention sweep**, because no vpay has been up for twelve months; **nothing erases a payer from a merchant's own copy of the identifiers, and nothing can** — the merchant received them in `customer.created` and every `customer.updated` before the erasure; `customer.deleted` carries the `cus_…` and the instant, which is the signal, and the review declined to add a second `customer.redacted` type saying the same thing, leaving the obligation to act on it as a contract question for the maintainer; and **one 24-hour window is open rather than closed** — `PostRequest::finish` stores a response body after its transaction commits, so an update that loses a race to a `DELETE` writes identifiers back into `idempotency_keys.response_body` until `sweep_expired` removes the row. **Updated again 2026-09-11 ([issue #70](https://github.com/vaam-apps/vpay/issues/70) item 3): `CustomerObject`'s `Debug` is hand-written, not derived.** Nothing formatted one before this change — it is a wire object, not a row — but `#[derive(Debug)]` on a type carrying a payer's name, email, phone and (since the GPS paragraph above, the same day) their postal address and coordinates was a redaction with a hole in it waiting for the first `{:?}` in a `tracing` field or an `anyhow` chain. The impl redacts `name`/`email`/`phone` as character counts, the same judgement `vpay_db::CustomerRow`'s own hand-written `Debug` already makes and for the same reason (an operator needs to know _which_ identifiers are present, not their values); a first pass at this issue redacted those three and then printed `address` un-redacted through `AddressObject`'s derived `Debug`, missing the field landed that same day that is at least as identifying as a name. `AddressObject` now hand-redacts too — a component count, matching `CustomerRow`'s treatment of the same eight columns, with the GPS pair counted as one component since the pair, not either half, is the value. Re-adding `#[derive(Debug)]` to either type is not a runtime regression to catch: it is `E0119` (conflicting implementations) at `cargo check`, confirmed by mutation. `a_customer_object_debug_output_redacts_personal_identifiers` builds a fixture with a full address and both coordinates and asserts none of the raw values it set appear — checked against the actual fixture literals, not stand-ins, after a mutation (un-redacting just `name`) showed the first version of this test would have passed with the payer's real name printed in the clear because two of its three negative assertions checked strings (`"John Doe"`, `"john@example.com"`) the fixture never contained |
| `just test-e2e` | ✅ | **Re-measured 2026-09-10 by the exp37 sabotage review, on `ff7a8e8`, and it is the first run since `demo_dashboard_port` became a variable (#94): 18 Cypress tests across four specs, 18 passing, 0 failing, 0 skipped, exit 0** (read from a file, not from a banner) — `checkout.cy.ts` (1), `dashboard.cy.ts` (8), `shop-hosted.cy.ts` (3) in pass 1; `shop-embedded.cy.ts` (**6**, was 4) in pass 2. Run beside the author's own `just demo` stack, entirely on non-default ports (`demo_project=exp37-review`, dashboard 13300, shop 13301, checkout 13380, api 18080, orange 18082, receiver 18083), which is exactly what #94 made possible and what the exp37 implementer recorded as blocked; the stack was torn down by `down -v` and the author's was untouched throughout. Wall clock 2 m 03 s + 24 s of Cypress. **Re-measured 2026-09-07 (exp28), and the recipe grew a step.** Green from nothing on that branch's final head, in an isolated compose project on non-default ports: **16 Cypress tests across four specs, 16 passing, 0 failing, 0 skipped, exit 0** — `checkout.cy.ts` (1), `dashboard.cy.ts` (**8**, was 3; 7 as exp28 delivered it, before the review below), `shop-hosted.cy.ts` (3) in pass 1; `shop-embedded.cy.ts` (4) in pass 2 — with the stack torn down by `down -v` and zero containers and zero volumes left behind. The new step is `just demo-staff`, run after the readiness probes: `dashboard.cy.ts` now signs in, and there is nobody to sign in as until `vpay-server staff add` has created one. **The recipe FAILS if it produced no password rather than letting the spec skip** — a dashboard spec that quietly tested a logged-out redirect six times would read exactly like a passing suite. The stack is nine services now, not eight: `dashboard` joined `demo_services`, because it has something to show. Two defects in this wiring were found by running it and are recorded in [plans/exp28-dashboard-pages-notes/opus.md](../../plans/exp28-dashboard-pages-notes/opus.md): the sub-invocation of `demo-staff` inherited the exported environment but **not** the recipe's variable overrides, so it addressed the DEFAULT compose project — somebody else's stack — until every override was repeated on it; and Cypress's default test isolation cleared the session between tests, which `testIsolation: false` fixes for reasons the spec states rather than assumes. **Changed 2026-09-04 (Step 9, lane 6): it now brings up a stack the specs can pass against.** It used to start `compose.yml -f compose.e2e.yml` alone, which registers no merchant anybody holds a private key for, so every spec that mints anything answered `invalid_client` — **`checkout.cy.ts` could not have passed under this recipe since Step 5c**, and that is recorded here rather than left as folklore. It now depends on `gen-demo-keys`, `build-sdk-node` and `build-checkout-browser`, adds `-f compose.demo.yml`, waits for `/healthz` and for the dashboard, the shop and the checkout page by name, runs BOTH Cypress passes and tears the stack down with `down -v`. CI's `e2e` job was fixed the same way, and one `.gitignore` line with it. Green from nothing in the `vpay-ci` VM on 2026-09-04: **11 tests across four specs, 0 failing, 0 skipped, exit 0** — `checkout.cy.ts` 13 s (1), `dashboard.cy.ts` 0.4 s (3), `shop-hosted.cy.ts` 1 m 18 s (3) in pass 1; `shop-embedded.cy.ts` 13 s (4) in pass 2 (`VPAY_E2E_FRAMED=1`). **Run green once, from nothing, in the VM** — not three times, and nothing here was measured for flakiness **New 2026-09-11: fixture ports derived from `demo_fixture_port`, issue #106 item 3.** `test-e2e` and `e2e-specs` previously hardcoded `CHECKOUT_BROWSER_PORT`=4180 and `VPAY_E2E_FRAME_FIXTURE_PORT`=4181 — unlike the seven compose ports beside them — so a concurrent `just test-e2e` in another worktree took both and failed on `EADDRINUSE` or `ECONNREFUSED` (observed by the exp43 review, 2026-09-10, against a real second run). Both now derive from `demo_fixture_port` (default 4180, second server at `+ 1`) and are exported to the Cypress task runner. **Reviewed, not re-run end-to-end** (Docker unavailable on this host at review time): `just --list` and `just --dry-run test-e2e`/`e2e-specs` parse and interpolate cleanly, `bash -n` accepts the rendered scripts, and both env var names were checked character-for-character against `checkoutBrowserServer.ts`, `examples/checkout-browser/serve.mjs` and `frameFixtureServer.ts`. The collision itself was reproduced cheaply, without a full e2e run: two `node serve.mjs` instances on `CHECKOUT_BROWSER_PORT=4180` collide with `Error: listen EADDRINUSE: address already in use :::4180`; two on distinct bases (4180, 5000) both bind. **No guard** stops `demo_fixture_port`/`demo_fixture_port + 1` from being set to a value that collides with `demo_orange_port`, `demo_receiver_port` or any other `demo_*` port — consistent with how the other six port variables already behave, none of which are cross-checked either. Items 1 and 2 of #106 (stub mapping priority, hidden-input encoding) are untouched by this change and #106 stays open for them. |
| `just audit-web` + CI `web` audit step | ✅ | New 2026-09-03. `cargo deny`'s counterpart for the JavaScript half of the repo, and the answer to 21 Dependabot alerts on `master`. **What is gated:** two `pnpm audit` runs at `--audit-level=high` — first `--prod` (the production graph alone: what `@vaam-apps/vpay-sdk`/`@vaam-apps/vpay-stripe-js` hand a merchant and what `frontends/Dockerfile` ships), then the whole workspace with dev dependencies included. Dev dependencies are gated on purpose: **all sixteen** advisories this pass cleared were in build/test tooling, so a `--prod`-only gate would have been green over every one of them. High and critical fail the recipe; moderate is reported by a bare `pnpm audit` but does not fail it — a ceiling chosen so that the weekly moderate in a transitive dev dependency does not train people to reach for the ignore list. **What is ignored: nothing.** `pnpm.auditConfig.ignoreCves` is absent from `package.json` and no advisory is suppressed; `pnpm audit` on this tree reports `No known vulnerabilities found` at every severity, down from 16 (1 critical, 5 high, 10 moderate) on `master` — and `--prod` alone was 3 high + 2 moderate there, all through Next's exactly-pinned `postcss@8.4.31` and `sharp@0.34.5`. Cleared by version bumps where a release existed (`vitest@2.1.9 → 3.2.7` in all seven packages that use it, clearing the one **critical**, GHSA-5xrq-8626-4rwp/CVE-2026-47429; `next@15.5.23 → 15.5.25`, whose optional `sharp` range widens to `^0.35.4` and so clears GHSA-f88m-g3jw-g9cj without an override; `cypress@13.17.0 → 15.21.1`, which drops `extract-zip` — GHSA-jmr9-qjv8-65gv has **no patched version at all**, so no 13.x or 14.x could have cleared it — and moves to `@cypress/request@4`, taking `qs` to 6.16.0 and dropping its `uuid` dependency entirely), and by two documented `pnpm.overrides` where no release existed — `next>postcss ^8.5.28` (Next pins 8.4.31 exactly on every 15.x and 16.x) and `@storybook/addon-actions>uuid ^11.1.1` (the bump landed in Storybook 9, a major migration of `frontends/packages/ui`) — plus a third, `vite ^6.4.3`, which is a **compatibility** pin and not an advisory fix: vite 7.3.6, what pnpm picks without it, is patched too, but it leaves `@storybook/{builder,react}-vite@8.6.18` (peer `^4\|\|^5\|\|^6`) on an unmet peer while vitest 3 wants `^5\|\|^6\|\|^7`, and pnpm hoists one copy. 6.4.3 is the only version in that intersection that is also patched, so it is a ceiling as much as a floor — if a 6.4.x advisory ever lands with the fix only in 7.x, this pin has to go and the Storybook 9 migration becomes urgent. Removing all three overrides and reinstalling was measured, not assumed: 5 advisories (2 high, 3 moderate) come straight back. Each override carries its advisory IDs and a removal condition in `package.json`'s `"//pnpm"` block. **Verified on the authoring machine, not in CI** (no run of this branch exists): `pnpm install --frozen-lockfile` clean, `just lint-web` clean across all fourteen TypeScript projects, `pnpm -r test` **248 passed** on vitest 3 (`sdks/nodejs` 150, `sdks/stripe-js` 88, `frontends/packages/tokens` 3, `frontends/packages/api-client` 4, `frontends/packages/ui` 3, `frontends/apps/dashboard` 0 `--passWithNoTests`), `pnpm --filter @vpay/ui build-storybook` and `pnpm --filter @vpay/dashboard build` both succeed on the pinned vite 6.4.3 / postcss 8.5.28, and `cypress install` + `cypress verify` succeed on 15.21.1 (Node engine `^20.1.0 \|\|^22.0.0 \|\|>=24.0.0`; `.nvmrc` was 22.11.0 when this was measured and is **22.23.2** since 2026-09-05, which that range also satisfies). **248, not the 247 the Step 5c note above records** — that difference predates this pass and is not caused by it: `sdks/stripe-js/src/client.test.ts` gained `strips a long run of trailing slashes in linear time` after `b57e0ce`, the commit that note was measured on, and it is on `master` today. **What this row does not claim:** nothing here has run on a GitHub runner, and no Cypress _spec_ was executed against a stack on this tree — only `cypress verify`. Whether Cypress 15 runs the two existing specs is unproven until the `e2e` job runs **Updated 2026-09-04: a registry outage is told apart from an advisory.** On 2026-09-04 the audit endpoint timed out or answered 503 for about two hours and failed seven consecutive CI runs of a branch whose lockfile had not changed (`ERR_SOCKET_TIMEOUT` in every log, a finding in none). Each `pnpm audit` now runs with a 180 s fetch timeout and up to four attempts 90 s apart, retried **only** when the output carries the registry's own failure signatures; an advisory fails at once with pnpm's report, and a registry that never answers fails with `REGISTRY UNREACHABLE — the audit did not run`. Neither passes: an unreachable audit is an audit that did not run. The retry loop is shell in the recipe, exercised by hand against the live endpoint on 2026-09-04 and by nothing else — there is no test for it, and CI's `web` job is the evidence that the happy path still gates. **Updated 2026-09-10 (exp41): `vitest` `^3.2.7` → `^4.1.11` everywhere, and the honest reading of this gate is that it did not catch the advisory that forced the bump.** GHSA-82fw-gwwq-j7x9 ("Path Traversal / Arbitrary File Read via `@vitest/mocker` Redirect Mock", published 2026-09-08) opened **twelve** Dependabot alerts on `master` — #26–#32 and #34–#36 for `vitest` in the ten manifests that declare it (`examples/shop`, `frontends/apps/{checkout,dashboard}`, `frontends/packages/{api-client,config,tokens,ui}`, `sdks/{nodejs,stripe-compat,stripe-js}`), plus #33 (`vitest`) and #25 (`@vitest/mocker`) against `pnpm-lock.yaml`. Vulnerable range `>= 2.1.0, < 4.1.11`, first patched `4.1.11`; all ten manifests and the lockfile now name it, and the lockfile resolves exactly one `vitest` and one `@vitest/mocker`, both `4.1.11`. (`@vitest/expect`, `@vitest/spy`, `@vitest/utils` and `@vitest/pretty-format` remain in the lockfile at 3.2.4 as well, through `storybook@10.6.0`; none of those four is named by the advisory.) **The advisory is _moderate_, so `just audit-web` was green over it for the two days the alerts were open — this row's ceiling working exactly as written above, not a defect, but worth saying out loud.** Measured in both directions on `master`'s lockfile (`git show 970bfe0:pnpm-lock.yaml` installed into a throwaway copy, Node 22.23.2): the recipe's two runs, `pnpm audit --audit-level=high --prod` and `--audit-level=high`, exit **0**; `--audit-level=moderate` exits **1** and names both packages. On this tree `--audit-level=moderate` exits 0 with `No known vulnerabilities found`. So for a moderate advisory in dev tooling the effective gate is **Dependabot, not this recipe**, and nothing this repository runs on a pull request would have failed. Whether the `--audit-level=high` ceiling should move is left where it was put: a maintainer's call, with the reasoning already in the justfile. **The bump was not free, and the draft that made it reported a green it had not seen.** `@vpay/ui` fails 6 runs in 10 on vitest 4 and 0 in 10 on vitest 3, same machine and identical `jsdom@25.0.1`/`@testing-library/react@16.3.2`/`react@19.2.8` — see the `@vpay/ui` row and [opus-review.md](../../plans/exp41-vitest-advisory-notes/opus-review.md). No vitest **configuration** migration was owed, and that was established against all fourteen headings of Vitest 4's migration guide rather than the two the draft checked: the repository has no coverage provider, no snapshots, no automocks, no `poolOptions`, no `workspace`/`projects`, no `@vitest/browser`, no custom reporter, no `deps`/`server.deps`, and no test options passed as a third argument — the one form v4 would ignore silently. `vite@6.4.3` already satisfies v4's Vite ≥ 6 floor. Gates on the final head: `just ci` exit 0 — `test-rust` 1658 passed / 0 skipped (equal to `master`'s; no Rust changed), `test-doc` 111 passed / 1 ignored, `verify-ignored` 45 binaries / 1658 total, `verify` twelve gates, `test-web` **1284 passed, 0 skipped across 96 files**. **Updated 2026-09-11 (issue #103, `claude/b1a-ci-gates` review):** the leading “What is gated” line above is stale as of this date — the `ci` recipe and CI's `web` job both now run `pnpm audit` at `--audit-level=moderate`, not `--audit-level=high`, and `audit-web` is now also a dependency of `just ci` (it was already a separate CI step). Verified `just audit-web` clean (`No known vulnerabilities found`, both `--prod` and whole-workspace) on this tree today, and the mechanism reproduced against a throwaway `vitest@3.2.7` install outside the workspace: `--audit-level=high` exits 0 over GHSA-82fw-gwwq-j7x9 (2 moderate findings), `--audit-level=moderate` exits 1 naming both. See the issues #103/#125 entry above for the full account. |
